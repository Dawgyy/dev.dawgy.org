// Tailwind v4 runs through the @tailwindcss/vite plugin — no PostCSS plugins needed.
// This empty config stops Vite from walking up to a parent v3 PostCSS config.
export default {
  plugins: {},
};
